import { StyleSheet } from 'react-native';
import { BLACK_HEADER } from './colors';

export const textStyles = StyleSheet.create({
  header: {
    color: BLACK_HEADER,
    fontSize: 24,
    padding: 8,
  },
});
